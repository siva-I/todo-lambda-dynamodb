{
  "name": "todo-lambda-dynamodb",
  "keywords": [
    "aws",
    "dynamodb",
    "lambda",
    "aws lambda",
    "save todo",
    "todo store",
    "todo"
  ],
  "private": true,
  "dependencies": {
    "uuid": "3.3.2"
  },
  "devDependencies": {
    "serverless": "1.30.1",
    "standard": "11.0.1"
  },
  "scripts": {
    "test": "standard",
    "deploy": "standard --fix && sls deploy",
    "preinstall": "npm config set save-exact=true"
  }
}
